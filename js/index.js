// Prevent us from attempting to use variables
// that are not declared
"use strict"

/*

const array9000 = [4, 5, 1, 0, 9, 3, 2, 8, 6, 7]
for (let i = 0; i < array9000.length; i++) {
    console.log(array9000[i]);
}

let sentence = ['words', 'in', 'a', 'sentence']
for(let words of sentence) {
    console.log(words);
}

let words = ['w', 'o', 'r', 'd', 's']
words.forEach(value => {
    console.log(value); 
});

let oldSentence = ['words', 'in', 'a', 'sentence'];
let newSentence = ['so', 'this', 'is', 'coding'];
for (let i = 0; i < oldSentence.length; i++) {
    oldSentence[i] = newSentence[i];
    console.log(oldSentence);
}

let letters = ['l', 'e', 't', 't', 'e', 'r', 's']
let srettel = ['s', 'r', 'e', 't', 't', 'e', 'l']
letters.forEach((value, index) => {
    letters[index] = srettel[index];
});
console.log(letters);

*/
